/** Class defining point on the board */
export class Point {
  x: number;
  y: number;

  /**
   * @param x - horizontal position coordinate
   * @param y - vertical position coordinate
   */
  constructor(x: number, y: number) {
    this.x = x;
    this.y = y;
  }
}
